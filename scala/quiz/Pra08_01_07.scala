package quiz

// 行列をリストのリストで表す.
// 転置行列(transposed matrix)を求めるにはどうすれば良いか．
object Pra08_01_07 {
  def main(args: Array[String]): Unit = {
    val matrix = List(List(3, 1, 4, 1), List(2, 7, 1, 8), List(0, 5, 7, 7))

  }
}
